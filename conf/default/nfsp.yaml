defaults:
  - agent: kuhn_poker
  - env: kuhn_poker
  - algorithm: nfsp
  - logging: tensorboard
  - _self_

hydra:
  searchpath:
    - file://conf/

run_name: "kuhn_poker/nfsp/default_run"
seed: 100
